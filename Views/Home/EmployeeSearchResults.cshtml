@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    Layout = ViewBag.Layout as string;
}
@model Proje4.Models.CustomHomeViewModel;
<h1> Search Results</h1>
<h3>Results for "@Model.searchQuery"</h3>

@if (Model.Tasks.Any())
{
    <h2>Task Results</h2>
    <table class="table">
        <thead>
            <tr>
                <th>
                    Name
                </th>
                <th>
                    Description
                </th>
                <th>
                    Completed
                </th>

                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Tasks)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Description)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Completed)
                    </td>

                    <td>
                        <a asp-controller="Tasks" asp-action="ViewTask" asp-route-id="@item.Id">Details</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}

@if (Model.Messages.Any())
{
    <h2>Message Results</h2>
    <table class="table">
        <thead>
            <tr>
                <th>
                    Text
                </th>
                <th>
                    From
                </th>
                <th>
                </th>
                <th>                </th>

                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Messages)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Text)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.User.Name)
                    </td>

                    <td>
                        <a asp-controller="Messages" asp-action="Details" asp-route-id="@item.Id">Details</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}



